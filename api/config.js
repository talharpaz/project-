/**
 * ==============================================
 * הגדרות API
 * ==============================================
 * 
 * קובץ זה מכיל הגדרות לחיבור ל-API.
 * שנה את USE_REAL_AI ל-true כדי להפעיל את OpenAI.
 * 
 * ==============================================
 */

// האם להשתמש ב-AI אמיתי (OpenAI)?
// false = מערכת כללים (בחינם, עובד אופליין)
// true = OpenAI (עולה כסף, צריך אינטרנט)
export const USE_REAL_AI = false;

// הגדרות נוספות
export const API_CONFIG = {
  // נקודת הקצה של ה-API שלנו
  endpoint: '/api/chat',
  
  // זמן המתנה מקסימלי (מילישניות)
  timeout: 30000,
  
  // מודל OpenAI (gpt-3.5-turbo זול יותר, gpt-4 חכם יותר)
  model: 'gpt-3.5-turbo'
};

